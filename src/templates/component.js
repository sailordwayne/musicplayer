/** @jsx jsx */
import { css, jsx } from '@emotion/react'

const Replace = () => {
  return (
    <div className="Replace" css={CSS}>
      {/*  */}
    </div>
  )
}

const CSS = css``

export default Replace
